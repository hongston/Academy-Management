import { all, call } from 'redux-saga/effects'

export default function* SagaRunner() {
  yield all([
    // call(AddTab),
    // call(GetUserInfoFromDB),
    // call(GetOrderDataFromDB),
    // call(GetCompanyInfoFromDB),
    // call(RegisterAccount),
    // call(DeleteAccount),
    // call(GetSellersDataFromDB),
    // call(ScrapOrders),
    // call(GetUsersInCompany),
    // call(ChangeOrderState),
    // call(GetUseDataFromDB),
    // call(GetWorkDataFromDB),
    // call(DeleteWorkData),
    // call(PreGetInquiryDataFromDB),
    // call(PreScrapInquiries),
    // call(PreGetAllCalcData),
    // call(PreSendInquiryAnswer)
  ]);
}